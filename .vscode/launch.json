{
   "version": "0.2.0",
   "configurations": [
      {
         // "command": "./node_modules/.bin/astro dev",
         "command": "bun run format && ./node_modules/.bin/astro dev",
         "name": "Development server",
         "request": "launch",
         "type": "node-terminal"
      }
   ]
}
